"use client";

import React from "react";
import LoginPage from "../login/page";

export default function RegisterPage() {
  // Direct back to unified auth client form matching backend sync functionality
  return <LoginPage />;
}
